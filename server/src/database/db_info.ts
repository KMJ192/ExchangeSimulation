import { switching } from '../switch/switch';

//DB의 정보, ID, Password 등은 gitignore
export let db_connect = {
    host : "localhost",
    port : "3306",
    user : "root",
    password : "1234",
    database : switching
}
